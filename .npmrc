# partyserver peers @cloudflare/workers-types v4; wrangler optionally peers v5.
# workers-types is types-only (no runtime), so this conflict is cosmetic. Relax
# peer resolution so both npm install (local) and npm ci (CI) succeed.
legacy-peer-deps=true
