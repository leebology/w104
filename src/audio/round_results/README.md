# round_results

Loops on the host screen through the **scoring reveal**, and carries on into
whichever standings screen follows it for as long as that screen's own folder is
empty — which is how the podium ends up playing the reveal's music.

Drop one `.mp3` or `.ogg` in here. The filename does not matter.
