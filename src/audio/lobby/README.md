# lobby

Loops on the host screen through the **lobby and team select** — everything
before the category vote, where the `gameplay/` track takes over.

Drop one `.mp3` or `.ogg` in here. The filename does not matter.
